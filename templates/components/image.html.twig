<div {{ attributes }} data-poll class="mt-3 flex justify-center items-center relative min-w-96 min-h-96 overflow-hidden rounded-full object-contain">
        <img class="object-cover" src="{{ img_folder }}/{{ this.getRandomImageName }}" alt="">
        <div id="cover" class="absolute top-0 bottom-0 left-0 right-0 opacity-0 hover:opacity-100 flex justify-center items-center text-center bg-blue-950 bg-opacity-50 text-white transition-all">{{ this.getName }}</div>
</div>
